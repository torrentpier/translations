Hei, {{ username }},

Du mottar denne e-posten fordi du følger emnet {{ topic_title }} på {{ sitename }}. Dette emnet har mottatt et svar siden siste besøk. Du kan bruke følgende lenke for å se svarene som er laget, ingen flere varsler vil bli sendt før du besøker emnet:

{{ u_topic }}

Hvis du ikke lenger ønsker å følge dette emnet, kan du enten klikke på "Slutt å følge dette emnet"-lenken som finnes nederst i emnet ovenfor, eller ved å klikke på følgende lenke:

{{ u_stop_watching_topic }}

{{ email_sig }}
