こんにちは、{{ username }}様、

このメールは、{{ sitename }}でトピック「{{ topic_title }}」を監視しているため送信されています。 このトピックには、前回の訪問以降に返信がありました。 以下のリンクから返信を確認できます。トピックを訪問するまで、これ以上通知は送信されません：

{{ u_topic }}

このトピックの監視をやめたい場合は、上記トピックの下部にある「このトピックの監視を停止する」リンクをクリックするか、以下のリンクをクリックしてください：

{{ u_stop_watching_topic }}

{{ email_sig }}
