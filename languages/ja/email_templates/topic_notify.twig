こんにちは、{{ username }}様、

このメールは、{{ sitename }}でトピック{{ topic_title }}を監視しているために受信しています。 このトピックは、前回の訪問時から返信を受けています。 以下のリンクを使用して返信を表示できます。トピックに訪問するまで、これ以上の通知は送信されません：

{{ u_topic }}

このトピックの監視を終了する場合は、上記のトピックの下部にある「このトピックの監視を停止する」リンクをクリックするか、以下のリンクをクリックしてください：

{{ u_stop_watching_topic }}

{{ email_sig }}
