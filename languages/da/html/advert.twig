<div id="infobox-body">
	<h2 class="bold">Annoncering på vores hjemmeside</h2>
	<p>For annoncering kontakt venligst: <strong><a href="mailto:{{ config('mail.addresses.advert') }}" class="gen">{{ config('mail.addresses.advert') }}</a></strong></p>
</div>
